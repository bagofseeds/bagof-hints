# bagof-hints

Reusable typing hints for `bagof` Python projects.

This package includes:

- general-purpose typing aliases for one-or-many values
- reusable JSON, path, and scalar typing hints
- tests that verify `mypy` understands the exported hints
