pytest>=7
mypy==1.14.1
numpy
# dask installs on a CPU runner and exercises the array submodule's
# library-present path. cupy is deliberately absent: it requires a CUDA
# toolchain and cannot be installed on a CPU runner, so its library-present
# branch is only ever type-checked.
dask[array]
